import React from "react";

interface IUseContainer {}
interface IUseContainerProps {}

const useContainer: IUseContainer = (props: IUseContainerProps) => {
  return {};
};

export default useContainer;
